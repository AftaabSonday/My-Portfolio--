{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "A basic portfolio website which is made using HTML, CSS need to update it further using bootstrap by giving responsiveness",
  "main": "script.js",
  "scripts": {
    "start": "http-server -c-1 .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
